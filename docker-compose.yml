services:
  lead-api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DB_PATH: /app/data/leads.db
    volumes:
      - ./data:/app/data
    restart: unless-stopped
